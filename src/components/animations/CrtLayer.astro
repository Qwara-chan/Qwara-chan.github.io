---
---
<!-- 全站 CRT 屏层：扫描线 + 暗角 + 滚动光带 + 微闪烁（纯 CSS 零 JS）。
     强度由 :root/.dark 的 --crt-* 变量控制；样式全部在 global.css。
     屏级效果，不占「每表面 ≤2 纹理」预算；reduced-motion 由全局 kill 块归零。 -->
<div data-crt-layer aria-hidden="true">
  <div data-crt-roll></div>
</div>
