---
import { getLangFromUrl, useTranslations, getLocalizedPath, getSwitchHref } from '@i18n/utils';
import type { Lang } from '@i18n/ui';

interface Props {
  lang?: Lang;
}

const lang = Astro.props.lang ?? getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const navItems = [
  { key: 'nav.home' as const, href: getLocalizedPath('', lang) },
  { key: 'nav.blog' as const, href: getLocalizedPath('blog', lang) },
  { key: 'nav.archive' as const, href: getLocalizedPath('archive', lang) },
  { key: 'nav.now' as const, href: getLocalizedPath('now', lang), className: 'hidden lg:inline-flex' },
  { key: 'nav.projects' as const, href: getLocalizedPath('projects', lang) },
  { key: 'nav.friends' as const, href: getLocalizedPath('friends', lang) },
  { key: 'nav.about' as const, href: getLocalizedPath('', lang) + '#about', hash: 'about' },
];

const currentPath = Astro.url.pathname.replace(/\/$/, '') || '/';
const currentHash = Astro.url.hash.replace(/^#/, '');
---

<header
  id="navbar"
  class="fixed top-0 left-0 right-0 z-50 transition-all duration-300"
  data-navbar
>
  <div
    class="navbar-bg absolute inset-0 transition-opacity duration-300 opacity-0 pointer-events-none"
    data-navbar-bg
  ></div>
  <div class="container-px container-mx relative">
    <nav class="flex items-center justify-between h-16 transition-all duration-300" data-nav-inner>
      <a href={getLocalizedPath('', lang)} class="flex items-center gap-2 font-bold text-lg tracking-tight">
        <img
          src="/avatar.png"
          alt=""
          class="w-8 h-8 rounded-lg transition-transform duration-300 hover:rotate-12"
          data-meow-rev="reverse-cat: follow the cat to /secret-cat/"
          width="32"
          height="32"
        />
        <span>Qwara</span>
      </a>

      <div class="hidden md:flex items-center gap-1">
        {navItems.map((item) => {
          const itemPath = item.href.replace(/#.*$/, '').replace(/\/$/, '') || '/';
          const isActive = item.hash
            ? currentPath === itemPath && currentHash === item.hash
            : currentPath === itemPath || (itemPath !== '/' && currentPath.startsWith(itemPath + '/'));
          return (
            <a
              href={item.href}
              class:list={[
                item.className,
                'px-4 py-2 text-sm rounded-full transition-all duration-300',
                isActive ? 'text-[var(--accent)]' : 'text-muted hover:text-[var(--fg)]',
              ]}
            >
              {t(item.key)}
            </a>
          );
        })}
      </div>

      <div class="flex items-center gap-2">
        <a
          href={getLocalizedPath('search', lang)}
          class="hidden sm:flex items-center justify-center w-9 h-9 rounded-full border border-default text-muted hover:text-[var(--fg)] hover:border-strong transition-all duration-300"
          aria-label={t('nav.search')}
        >
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="11" cy="11" r="8" /><path d="m21 21-4.3-4.3" />
          </svg>
        </a>

        <button
          class="theme-toggle hidden sm:flex items-center justify-center w-9 h-9 rounded-full border border-default text-muted hover:text-[var(--fg)] hover:border-strong transition-all duration-300"
          aria-label="Toggle theme"
        >
          <svg class="sun-icon w-4 h-4 hidden dark:block" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="4" /><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41" />
          </svg>
          <svg class="moon-icon w-4 h-4 block dark:hidden" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z" />
          </svg>
        </button>

        <a
          href={getSwitchHref(Astro.url, lang === 'zh' ? 'en' : 'zh')}
          class="flex items-center justify-center px-3 h-9 rounded-full border border-default text-muted hover:text-[var(--fg)] hover:border-strong transition-all duration-300 text-xs font-mono font-bold"
        >
          {lang === 'zh' ? 'EN' : '中'}
        </a>

        <button
          class="md:hidden flex items-center justify-center w-9 h-9 rounded-full border border-default text-muted hover:text-[var(--fg)] transition-all duration-300"
          aria-label="Menu"
          data-menu-toggle
        >
          <svg class="menu-open w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
            <path d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg class="menu-close w-4 h-4 hidden" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
            <path d="M18 6 6 18M6 6l12 12" />
          </svg>
        </button>
      </div>
    </nav>
  </div>
</header>

<div
  class="mobile-menu fixed inset-0 z-[55] bg-[var(--bg)]/95 flex flex-col items-center justify-center gap-10 opacity-0 pointer-events-none transition-opacity duration-300 md:hidden backdrop-blur-xl"
  data-mobile-menu
>
  {navItems.map((item) => (
    <a
      href={item.href}
      class="text-3xl font-bold tracking-tight text-muted hover:text-[var(--accent)] transition-colors duration-300"
      data-menu-link
    >
      {t(item.key)}
    </a>
  ))}
  <div class="flex items-center gap-3 mt-4">
    <a
      href={getLocalizedPath('search', lang)}
      class="flex items-center justify-center w-11 h-11 rounded-full border border-default text-muted hover:text-[var(--accent)] hover:border-[var(--accent)] transition-all duration-300"
      aria-label={t('nav.search')}
    >
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="11" cy="11" r="8" /><path d="m21 21-4.3-4.3" />
      </svg>
    </a>
    <button
      class="theme-toggle flex items-center justify-center w-11 h-11 rounded-full border border-default text-muted hover:text-[var(--accent)] hover:border-[var(--accent)] transition-all duration-300"
      aria-label="Toggle theme"
    >
      <svg class="sun-icon w-4 h-4 hidden dark:block" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="4" /><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41" />
      </svg>
      <svg class="moon-icon w-4 h-4 block dark:hidden" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z" />
      </svg>
    </button>
    <a
      href={getSwitchHref(Astro.url, lang === 'zh' ? 'en' : 'zh')}
      class="flex items-center justify-center w-11 h-11 rounded-full border border-default text-muted hover:text-[var(--accent)] hover:border-[var(--accent)] transition-all duration-300 text-xs font-mono font-bold"
    >
      {lang === 'zh' ? 'EN' : '中'}
    </a>
  </div>
</div>

<div class="h-16"></div>

<script>
  // Module-scoped AbortController: every init pass aborts the previous pass's
  // listeners. Without this, every view-transition would stack another click
  // listener on each `.theme-toggle` button — after 1 swap, two toggles fire per
  // click and cancel out (theme button looks dead).
  let navbarAbort: AbortController | null = null;

  function initNavbar() {
    navbarAbort?.abort();
    navbarAbort = new AbortController();
    const { signal } = navbarAbort;

    const navbar = document.querySelector('[data-navbar]');
    const navInner = document.querySelector('[data-nav-inner]');
    const navBg = document.querySelector('[data-navbar-bg]');
    const menuToggle = document.querySelector('[data-menu-toggle]');
    const mobileMenu = document.querySelector('[data-mobile-menu]');
    const menuOpen = document.querySelector('.menu-open');
    const menuClose = document.querySelector('.menu-close');
    const menuLinks = document.querySelectorAll('[data-menu-link]');

    let scrolled = false;
    let menuOpenState = false;

    function updateScrolled() {
      // When menu is open, keep navbar transparent (menu provides the background)
      const wantBlur = scrolled && !menuOpenState;
      if (navbar) {
        navbar.classList.toggle('backdrop-blur-md', wantBlur);
        navbar.classList.toggle('border-b', wantBlur);
        navbar.classList.toggle('border-default', wantBlur);
      }
      if (navInner) navInner.classList.toggle('h-14', scrolled && !menuOpenState);
      if (navBg) {
        navBg.classList.toggle('opacity-100', wantBlur);
        navBg.classList.toggle('opacity-0', !wantBlur);
      }
    }

    function handleScroll() {
      scrolled = window.scrollY > 20;
      updateScrolled();
    }

    window.addEventListener('scroll', handleScroll, { passive: true, signal });
    handleScroll();

    function toggleMenu(open: boolean) {
      menuOpenState = open;
      if (mobileMenu) {
        if (open) {
          mobileMenu.classList.remove('opacity-0', 'pointer-events-none');
          mobileMenu.classList.add('opacity-100');
        } else {
          mobileMenu.classList.add('opacity-0', 'pointer-events-none');
          mobileMenu.classList.remove('opacity-100');
        }
      }
      menuOpen?.classList.toggle('hidden', open);
      menuClose?.classList.toggle('hidden', !open);
      document.body.classList.toggle('overflow-hidden', open);
      updateScrolled();
    }

    menuToggle?.addEventListener('click', () => toggleMenu(!menuOpenState), { signal });
    menuLinks?.forEach((link) => link.addEventListener('click', () => toggleMenu(false), { signal }));

    const themeToggle = document.querySelectorAll('.theme-toggle');
    themeToggle.forEach((btn) => {
      btn.addEventListener('click', () => {
        const isDark = document.documentElement.classList.contains('dark');
        const newTheme = isDark ? 'light' : 'dark';

        if (!document.startViewTransition) {
          document.documentElement.classList.toggle('dark');
          localStorage.setItem('theme', newTheme);
          return;
        }

        document.startViewTransition(() => {
          document.documentElement.classList.toggle('dark');
          localStorage.setItem('theme', newTheme);
        });
      }, { signal });
    });
  }

  initNavbar();
  document.addEventListener('astro:after-swap', initNavbar);
</script>
