---
import TagPage from '@components/pages/TagPage.astro';
import { getTagStaticPaths } from '@utils/posts';

export const getStaticPaths = () => getTagStaticPaths('zh');

const { tag, posts } = Astro.props;
---

<TagPage tag={tag} posts={posts} />
